#management.endpoints.web.exposure.include=health,info
#management.info.env.enabled=true

#info.app.name=Hospital App

#info.app.description=The hospital app is a web-based platform that automates hospital operations, patient management.
#info.app.version.version=1.0.0

#doctor.name=Harry Potter
#clinic.name=Dentist

#Change Spring Boot embedded server port
server.port=7080git

#Set the context path of the application
#All request should be prefixed with /myfirstapp
#server.servlet.context-path=/myfirstapp

spring.datasource.url=jdbc:postgresql://localhost:5432/springboot_db
spring.datasource.username=dev_user
spring.datasource.password=password

spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true